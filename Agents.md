# TC264 摄像头四轮舵机车项目规则

## 项目与硬件边界

1. 本项目是基于英飞凌 TC264、IPS200 显示屏和总钻风摄像头的视觉四轮舵机智能车项目。CPU0 专注于图像处理算法，CPU1 专注于运动控制；修改前必须先确认代码所属核心及其数据交互边界。
2. 所有编译、烧录和硬件验证均由用户在英飞凌 ADS 中完成。代理只能进行与改动相称的静态检查，不能把静态检查表述为 ADS 编译、烧录或实车验证结果。
3. 左圆环的实际路线约束是：从圆环右上侧沿逆时针方向进环，逆时针行驶一圈后出环。分析车辆行为时必须以此方向为准。

## 文件与编码规则

4. 如果任务需要删除任何文件，必须立即停止删除操作，明确列出拟删除文件并请求用户同意；只有获得用户明确同意后才能删除。
5. 严禁新建任何 `.c` 或 `.h` 文件。所有业务代码改动必须基于以下目录中的现有文件：
   - `C:\Users\durian\Desktop\TC264\shexiangtou\Seekfree_TC264_Opensource_Library\code`
   - `C:\Users\durian\Desktop\TC264\shexiangtou\Seekfree_TC264_Opensource_Library\user`
6. 严禁修改逐飞 `device` 库及 `Seekfree_TC264_Opensource_Library\libraries` 下的逐飞库文件。
7. 新建或修改的文本文件必须采用 UTF-8 编码且严禁包含 BOM；提交前必须检查文件头，防止英飞凌 ADS 因 BOM 无法编译。
8. 所有新增或修改的代码必须添加数量合理、准确说明意图或边界条件的简体中文注释；禁止为了凑注释而逐行复述代码。

## 代码修改约束

9. 思考、过程说明、代码注释、提交说明和最终回答必须使用简体中文；不可避免的 API、寄存器、变量名和工具原名可以保留英文。
10. 实现必须兼顾嵌入式实时性、边界条件、跨核数据一致性、鲁棒性。
11. 舵机和电机参数均已通过实车校验，严禁修改，包括但不限于舵机中值/限位、方向权重、电机速度、PID/PD 参数、输出映射和相关标定常量。
12. PID 输出限幅固定为 `[-200, 200]`，严禁扩大、缩小或绕过该限幅。
13. 严禁修改斑马线、圆环和十字的识别、状态机、补线、控制或优先级逻辑。若任务可能间接影响这些逻辑，修改前必须先向用户说明并等待明确授权。
14. 必要时可以从 GitHub 查找优秀案例，但必须先核对其许可证、硬件平台、摄像头输入、坐标系、时序、内存占用和编译器差异，并将方案适配 TC264 双核架构；不得直接照搬其他 MCU 或上位机代码。

## Git 与交付规则

15. 每次由代理完成的修改都必须形成独立 Git 提交。提交前必须检查差异、UTF-8 无 BOM、暂存文件清单和 `git diff --check`；不得把未验证结果描述为已通过 ADS 或实车验证。
16. Git 中的业务代码范围仅限 `Seekfree_TC264_Opensource_Library\code` 和 `Seekfree_TC264_Opensource_Library\user`。压缩包、构建产物、测试副本、文档草稿及其他目录内容不纳入业务代码提交。本项目级 `Agents.md` 仅因用户本次明确要求而作为规则文件提交。
17. 用户可能同时临时调节速度、PD 等参数。提交时只暂存当前任务明确要求的文件，严禁提交这些临时数据；交付时必须逐项列出检测到但未提交的临时改动。无法判断改动归属时不得擅自暂存。
18. 当前工作区已有版本、删除项、未跟踪文件和既有未推送提交均不得因本规则文件的提交而被暂存、提交或上传；必须使用隔离提交或独立分支，确保远端只新增本次明确要求的规则文件改动。
19. 推送前必须再次核对提交相对目标远端基线的文件列表；只有 `Agents.md` 出现在本次规则提交中时才允许推送。
